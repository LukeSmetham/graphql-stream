type Subscription {
    """
    Subscribe to Stream Chat Client Events
    """
    clientEvents: Event
    """
    Subscribe to Stream Chat Client Events
    """
    channelEvents(channel: EntitySelector): Event
}
